spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: never  # schema.sql tiene PL/pgSQL con $$ — se ejecuta manualmente

logging:
  level:
    com.peribook: DEBUG
    org.springframework.security: DEBUG
