services:
  bff-web:
    build: .
    container_name: bff-web
    ports:
      - "8086:8086"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    networks:
      - peribook-net
    restart: unless-stopped

