spring:
  jpa:
    hibernate:
      ddl-auto: create-drop  # Dev: recrea schema en cada arranque
  docker:
    compose:
      enabled: false  # No queremos que Spring intente manejar compose él mismo

logging:
  level:
    com.peribook: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
<!-- 2026-07-09 -->
